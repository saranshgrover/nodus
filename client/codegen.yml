overwrite: true
schema: http://localhost:5000/graphql
config:
    avoidOptionals: true
generates:
    ./src/@types/graphql.ts:
        plugins:
            - typescript
            - typescript-resolvers
